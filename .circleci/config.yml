version: 2.1
jobs:
  build-development:
    docker:
      - image: cimg/base:2022.05
    steps:
      - add_ssh_keys:
          fingerprints:
            - 'SHA256:hQ5CqSmDxYcA4Lnw5PxRF1d+IMIvioaMeObtbnzFaaw'
      - run:
          name: Deploy app to server development
          command: |
            ssh -o StrictHostKeyChecking=no trias@203.175.10.195 "cd aplikasi/trijuna_retail_backend && ./build.sh"
workflows :
  basic-workflow:
    jobs:
      - build-development:
          filters:
            branches:
              only:
                - main